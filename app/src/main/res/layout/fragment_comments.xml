<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:isScrollContainer="true"
        android:background="@drawable/bottom_sheet_radius_background"
        >
        <include android:id="@+id/action_bar" layout="@layout/bottom_sheet_header"/>
        <ProgressBar
            android:id="@+id/progress_bar"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_gravity="center"
            />
        <com.example.market.recycler.EmptyRecyclerView
            android:id="@+id/comments_recycler_view"
            android:layout_width="match_parent"
            android:layout_height="400dp"
            android:layout_marginVertical="56dp"
            tools:listitem="@layout/comment_item"
            />
        <include android:id="@+id/comments_bottom_nav" layout="@layout/comments_bottom_nav" />
    </FrameLayout>
</layout>